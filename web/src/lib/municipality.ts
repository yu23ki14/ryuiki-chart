/**
 * `sites.municipality` は出典によって中身が違う。
 * 環境省 公共用水域の 290 地点では水域名（河川名・湖沼名）が入り、
 * それ以外の 62 地点では市区町村名が入る。列名と中身が一致していないので、
 * 画面では「水域・地域」と呼ぶ。
 *
 * `SiteList` / `SiteDetail` / `MapPage` / `TimeseriesExplorer` の4画面が同じ表示ラベルを
 * 使うので、どの1画面にも直書きせずここに置く（CLAUDE.md: 同じ値を複数の画面が
 * 別々に持たない）。特定の feature フォルダの配下ではなく `lib/` に置くのは、
 * どの画面も「この値の持ち主」ではないため（4画面のうちの1つの下に置くと、
 * 他の3画面がその画面のフォルダから import する不自然な依存になる）。
 * 指標・単位・注記・zone のような登録語彙ではなく、この4画面だけの表示上の言い換えなので
 * レジストリ（`@/lib/registry/`）には置かない（docs/plans/PHASE_B_INTAKE.md #6）。
 */
export const MUNICIPALITY_LABEL = "水域・地域";
