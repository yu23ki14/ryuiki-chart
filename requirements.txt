# これはレジストリビルド（scripts/r01_build_registry.py と scripts/registry/*.py）と
# Phase B 突合ハーネス（scripts/b01_derived_baseline.py / scripts/b02_derived_compare.py /
# scripts/reconcile/*.py）が実際に import するサードパーティ依存の固定リストであり、
# scripts/c*.py（収集スクリプト）全体の依存マニフェストではない。収集スクリプト側は
# 別途棚卸しが必要（このファイルの範囲外）。
#
# レジストリは「捨てて作り直せる」ことが要件（ADR-0001）。再構築には
#   pip install -r requirements.txt
#   .venv/bin/python3 scripts/r01_build_registry.py
# だけで足りる（詳細は registry/README.md）。
#
# バージョンは、このリポジトリの .venv に実際に入っているものをそのまま固定した
# （pytest はこのファイルを最初に足した時点で .venv に無かったので、
# 手元・CI いずれでも動作確認した pyenv グローバル環境のバージョンを固定した）。
PyYAML==6.0.3
pytest==8.2.0
